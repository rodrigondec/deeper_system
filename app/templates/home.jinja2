{% extends "layout.jinja2" %}

{% block content %}
    <div class="col-md-8">
        <h1>Video Listing</h1>
        <hr>
        <div class="col-md-10">
            <table class="table">
                <thead>
                    <th class="col-md-4">Name</th>
                    <th class="col-md-3">Theme</th>
                    <th class="col-md-1"></th>
                    <th class="col-md-1"></th>
                </thead>
                {% for video in videos %}
                <tr>
                    <td>{{ video.name }}</td>
                    <td>{{ video.theme.name }}</td>
                    <td>
                        <form action="/up" method="get" style="display: inline">
                            <input hidden type="text" name="id" value="{{ video.id }}">
                            <button class="btn btn-success" type="submit">
                                <i class="glyphicon glyphicon-thumbs-up"></i> {{ video.thumbs_up }}
                            </button>
                        </form>
                    </td>
                    <td>
                        <form action="/down" method="get" style="display: inline">
                            <input hidden type="text" name="id" value="{{ video.id }}">
                            <button class="btn btn-warning" type="submit">
                                <i class="glyphicon glyphicon-thumbs-down"></i> {{ video.thumbs_down }}
                            </button>
                        </form>
                    </td>
                </tr>
            {% endfor %}
            </table>
        </div>
    </div>
    <div class="col-md-4">
        <h1>Submit Video</h1>
        <hr>
        <form action="/add_video" method="get">
            <div class="form-group">
                <label>Name</label>
                <input type="text" name="name" class="form-control" placeholder="Name">
            </div>
            <div class="form-group">
                <label>Theme</label>
                <input type="text" name="theme" class="form-control" placeholder="Theme">
            </div>
            <input class="btn btn-success" type="submit" value="Send">
            <input class="btn btn-info" type="reset" value="Clear">
        </form>
    </div>

{% endblock content %}
